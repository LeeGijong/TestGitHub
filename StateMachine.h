#pragma once
//주인공 상태
enum {
	ACTOR_STATE_IDLE,
	ACTOR_STATE_JUMP,
	ACTOR_STATE_WALK,
	ACTOR_STATE_RUN,
	ACTOR_STATE_ATTACK,
	ACTOR_STATE_HURT,
	ACTOR_STATE_DIE,
	ACTOR_STATE_SKILL
};
//이동 방향
enum {
	MOVE_DIR_LEFT = -1,
	MOVE_DIR_RIGHT = +1,
};
//주인공 공격 상태
enum {
	ACTOR_ATTACK_SWORD,
	ACTOR_ATTACK_BULLET
};
//총알
enum {
	BULLET_DIR_LEFT = -1,
	BULLET_DIR_RIGHT = +1
};
//스킬
enum {
	SKILL_STATE_ALIVE,
	SKILL_STATE_DEAD,
	SKILL_DIR_LEFT = -1,
	SKILL_DIR_RIGHT = +1
};
// 점프 상태
enum {
	JUMP_STATE_UP,
	JUMP_STATE_DOWN,
	JUMP_STATE_LANDING,
	JUMP_STATE_DEF
};
//적 상태
enum {
	ENEMY_STATE_IDLE,
	ENEMY_STATE_WAIT,
	ENEMY_STATE_FOLLOW,
	ENEMY_STATE_ATTACK,
	ENEMY_STATE_HURT,
	ENEMY_STATE_DIE,
	
	ENEMY_STATE_WALK,
	ENEMY_STATE_LAND,
	ENEMY_STATE_UP
};